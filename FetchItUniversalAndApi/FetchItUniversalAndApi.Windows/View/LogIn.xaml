<Page
    x:Class="FetchItUniversalAndApi.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FetchItUniversalAndApi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:FetchItUniversalAndApi.ViewModel"
    mc:Ignorable="d">
<Page.DataContext>
        <viewModel:LogInViewModel/>
</Page.DataContext>
    <Grid Background="{StaticResource FetchitBackgroundColour}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Enter information:" FontSize="25"/>
            <TextBox Text="{Binding Username, Mode=TwoWay}" PlaceholderText="Username"/>
            <PasswordBox Password="{Binding Password, Mode=TwoWay}" PlaceholderText="Password" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="button_login" Content="Log in" Command="{Binding LogInCommand}"  Click="LoginButton_OnClick"/>
                <Button x:Name="button_register" Content="Register" Click="RegisterButton_OnClick" Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
