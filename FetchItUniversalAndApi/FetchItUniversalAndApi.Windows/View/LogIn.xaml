<Page
    x:Class="FetchItUniversalAndApi.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FetchItUniversalAndApi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:FetchItUniversalAndApi.ViewModel"
    mc:Ignorable="d">
<Page.DataContext>
        <viewModel:MainPageViewModel/>
</Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBox Text="{Binding Username, Mode=TwoWay}" PlaceholderText="Input username"/>
            <PasswordBox Password="{Binding Password, Mode=TwoWay}" PlaceholderText="Input password" />
            <Button x:Name="loginButton" Content="Log in" Command="{Binding LogInCommand}" Click="LoginButton_OnClick" HorizontalAlignment="Center" Margin="112,0"/>
            <Button x:Name="registerButton" Content="Register" Command="{Binding LogInCommand}" Click="RegisterButton_OnClick" HorizontalAlignment="Center" Margin="112,0,88,0" Width="100"/>
        </StackPanel>
    </Grid>
</Page>
