<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FetchItUniversalAndApi.View"
    xmlns:common="using:FetchItUniversalAndApi.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:FetchItUniversalAndApi.ViewModel"
    x:Name="pageRoot"
    x:Class="FetchItUniversalAndApi.View.ReportProfilePage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{StaticResource FetchitBackgroundColour}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.DataContext>
            <ViewModel:ReportProfileViewModel/>
        </Grid.DataContext>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
				Style="{StaticResource NavigationBackButtonNormalStyle}"
				VerticalAlignment="Top"
				AutomationProperties.Name="Back"
				AutomationProperties.AutomationId="BackButton"
				AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
				IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <TextBox HorizontalAlignment="Left" Margin="97,136,0,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding ReportMessage, Mode=TwoWay}" VerticalAlignment="Top" Height="191" Width="199"/>
        <TextBlock HorizontalAlignment="Left" Margin="97,76,0,0" Grid.Row="1" TextWrapping="Wrap" Text="You are reporting: " VerticalAlignment="Top" FontSize="14"/>
        <TextBlock x:Name="profileToReportTxtBlock" HorizontalAlignment="Left" Margin="215,76,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="14" Text="{Binding ProfileToReport.ProfileName, Mode=TwoWay}"/>
        <Button Content="Submit report" HorizontalAlignment="Left" Margin="94,372,0,0" Grid.Row="1" VerticalAlignment="Top" Command="{Binding SubmitReportCommand}"/>
        <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Left" Margin="221,372,0,0" Grid.Row="1" VerticalAlignment="Top" Click="cancelButton_Click"/>
        <TextBlock HorizontalAlignment="Left" Margin="392,76,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Logged in profile:" VerticalAlignment="Top" FontSize="14"/>
        <TextBlock x:Name="loggedInProfileTxtBox" HorizontalAlignment="Left" Margin="521,76,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="14" Text="{Binding LoggedInProfile.ProfileName, Mode=TwoWay}"/>
    </Grid>
</Page>
